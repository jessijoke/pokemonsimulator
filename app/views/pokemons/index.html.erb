<%= render :partial => "teams/pokemonlayout" %>

Check out my pokemons I am so 1337 <br />

<% @pokemons.each do |pokemon| %>
    <%= link_to(image_tag(pokemon.sprite), pokemon_path(pokemon.id)) %>
<% end %>